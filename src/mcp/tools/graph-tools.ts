import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

// Stub: graph tools have been split into graph-entity-tools, graph-relation-tools, graph-query-tools
export function registerGraphTools(_server: McpServer): void {
  // All tools moved to separate modules — this stub remains for backwards compatibility
}